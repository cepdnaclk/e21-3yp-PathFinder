import 'package:flutter_test/flutter_test.dart';

import 'package:pathfinder/main.dart';

void main() {
  test('PathFinderApp can be constructed', () {
    expect(const PathFinderApp(), isA<PathFinderApp>());
  });
}
